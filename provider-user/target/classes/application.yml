server:
  port: 7901 #程序启动之后的端口 也就是tomcat的端口
spring:
  application:
    name: provider-user #应用名称
eureka:
  client:
#    register-with-eureka: false
#    fetch-registry: false
    service-url:
      defaultZone: http://localhost:10000/eureka